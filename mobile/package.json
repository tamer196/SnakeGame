{
  "name": "neon-serpent-mobile",
  "version": "1.0.0",
  "description": "NEON SERPENT - Capacitor wrapper around the web build",
  "private": true,
  "scripts": {
    "sync": "cap sync",
    "open:android": "cap open android",
    "run:android": "cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "typescript": "5.9"
  }
}
